# 时序智配 Stitch 产品需求文档

> 版本：1.0
> 更新：2026-09-25
> 目标：交给 Stitch 生成完整的时序智配产品界面稿，覆盖主界面、二级页面、抽屉、弹窗、空状态和异常状态。
> 依据：当前 `Ruanchuang-main` 仓库的 Flutter 功能、`docs/时序智配Web端设计文档.md` 与已批准的“时间地图”视觉规范。

## 1. 产品定位

时序智配是面向高压知识工作者的本地优先智能日程工作台。它把任务、日历、当前精力和团队协作放在同一工作上下文中，在临时任务打乱计划时给出可解释的重排方案，让用户比较代价后再确认。

核心闭环：

```text
输入紧急任务
  -> 校验精确截止日期和时间
  -> 生成三种日程救援方案
  -> 比较移动任务、恢复缓冲和逾期风险
  -> 用户确认并写入日程
  -> 显示成功状态和撤销入口
  -> 在周/月复盘中显示接受与撤销历史
```

### 1.1 目标用户

- 需要同时管理任务、会议和个人精力的知识工作者。
- 经常被临时需求、延期会议或状态下降打断的个人用户。
- 需要共享空闲/忙碌信息并减少协调成本的小型团队。

### 1.2 产品目标

- 让用户在 10 秒内看懂今天的节奏、风险和下一步动作。
- 让紧急任务救援始终由用户确认，不静默覆盖原计划。
- 用真实状态区分远端同步、本地模式、加载、空数据、冲突和失败。
- 让日程变化可撤销、可追溯、可进入复盘。

### 1.3 非目标

- 不设计营销落地页或首屏宣传页，打开产品直接进入工作台。
- 不在当前版本声称已接入生理传感器、GPU 模型或第三方深度监听。
- 不引入第二套任务模型，不改变现有 FastAPI、SQLite 和本地优先数据策略。
- MCP、通知、认知画像等未接入能力必须显示为“待接入/暂不可用”，不能伪装成实时能力。

## 2. 信息架构与页面清单

### 2.1 一级导航

桌面侧栏分组：

| 分组 | 页面 | 路由语义 | 主动作 |
| --- | --- | --- | --- |
| 今天 | 专注 | `/focus` | 开始/暂停当前任务 |
| 今天 | 智能日历 | `/schedule` | 添加紧急任务并启动救援 |
| 规划 | 微任务 | `/micro` | 添加或导入微任务 |
| 协作 | 团队 | `/team` | 预约协作窗口 |
| 系统 | 我的 | `/profile` | 打开设置或账户入口 |

### 2.2 二级页面

从“我的”或日历更多菜单进入：

| 页面 | 路由语义 | 主要能力 |
| --- | --- | --- |
| 情绪与能量 | `/emotion` | 情绪打卡、今日状态、关怀建议 |
| 目标 | `/goals` | 新建目标、自动拆解、安排下一个任务 |
| 智能复盘 | `/review` | 周/月报告、救援历史、调度参数 |
| MCP 接入 | `/integrations` | 解析外部文本、预览并导入日程 |
| 蓝牙设备 | `/bluetooth` | 扫描、连接、断开设备 |
| 设备详情 | `/bluetooth/:deviceId` | 服务、特征值、读写和通知 |
| 诊断 | `/diagnostics` | 数据源、存储、调度性能和日志 |

### 2.3 覆盖范围

Stitch 必须至少生成以下画板/状态：

1. 桌面展开侧栏工作台。
2. 平板紧凑导航工作台。
3. 手机底部导航工作台。
4. 五个一级页面的默认、加载、空数据、错误和本地模式状态。
5. 七个二级页面的默认与关键空/错误状态。
6. 所有下文定义的抽屉、Bottom Sheet、Dialog、全屏方案比较页。
7. 浅色和深色主题；桌面、平板、手机三种尺寸。

## 3. 视觉与交互基线

### 3.1 视觉方向

关键词：冷静、可信、时间地图、低噪声、可解释、可撤销。

- 工作台而非营销页面；内容密度适中，优先支持扫描和重复操作。
- 毛玻璃只用于导航、状态层和抽屉分层，正文区域必须有实体对比度。
- 页面区块使用清晰的标题、指标、标签和状态徽标；不使用装饰性渐变球或大面积插画。
- 所有按钮、图标、标签和错误信息使用中文默认文案，同时保留英文文案位。
- 图标优先使用 Material/Lucide 语义图标；图标按钮必须有 tooltip 和无障碍名称。

### 3.2 颜色令牌

| 令牌 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `canvas` | `#EEF2F0` | `#101B1D` | 页面背景 |
| `ink` | `#183238` | `#E7F0ED` | 主文本、导航 |
| `surface` | `#FFFFFF` | `#1C2B2E` | 卡片、表面 |
| `recovery` | `#83AFA5` | `#8EC8B7` | 能量、恢复缓冲 |
| `deadline` | `#E5A15B` | `#F2B56E` | 截止压力、提醒 |
| `risk` | `#C85A4B` | `#F07A6A` | 逾期、冲突、失败 |
| `muted` | `#647579` | `#9CB0AE` | 辅助文本 |

### 3.3 布局与尺寸

- 桌面 `>=1200px`：260px 展开侧栏 + 顶部状态栏 + 主工作区；智能日历可显示右侧救援摘要。
- 平板 `720-1199px`：88px 紧凑 `NavigationRail`；救援摘要改为可展开面板。
- 手机 `<720px`：底部 `NavigationBar`；编辑、方案比较、设置统一使用 Bottom Sheet。
- 页面内边距 16/24/32dp，内容最大宽度 1320px。
- 容器圆角 24px，组件圆角 16px，控件圆角 12px；触控区域至少 44pt/48dp。
- 标题可使用仓库内 NotoSerifSC；正文、数据、按钮使用系统无衬线字体。

### 3.4 通用状态徽标

所有页面顶部或主区块可显示一个数据源徽标：

- `已同步`：远端读取/写入成功。
- `本地模式`：远端不可用或 5xx，使用本地兜底；核心日程功能仍可用。
- `加载中`：骨架或进度指示，不显示过期数据为实时数据。
- `需要登录`：鉴权缺失，给出登录入口，不把鉴权失败转换为空列表。
- `有冲突`：数据或调度冲突，展示问题数量和处理入口。

## 4. 全局壳层

### 4.1 桌面侧栏

- 顶部显示产品名“时序智配”和小型品牌圆点。
- 分组显示“今天 / 规划 / 协作 / 系统”。
- 当前导航项使用强调色背景和选中图标。
- 提供展开/收起按钮；收起时只显示图标，悬停显示 tooltip。
- 侧栏底部显示当前账户状态：头像、昵称、登录/游客标签。
- 预留通知入口但标记为“通知（预留能力，待接入）”，不表现成可用通知中心。

### 4.2 顶部栏

- 当前页面标题、日期或周期。
- 数据源状态徽标。
- 当前精力摘要：能量等级、情绪标签、恢复缓冲。
- 页面级主动作和更多菜单。
- 小屏将次要动作收进更多菜单，主动作始终可见。

### 4.3 手机底部导航

固定五项：专注、日程、微任务、团队、我的。底部安全区内显示，不能遮挡列表末尾内容。

## 5. 一级页面规格

### 5.1 专注页 `/focus`

#### 页面目的

把用户当前最需要做的事、接下来四小时的节奏和精力建议放在同一屏，减少进入日历的成本。

#### 主界面

- 顶部：日期“今天”、工作台标题、同步状态。
- 当前任务主卡：任务标题、标签、开始/结束时间、剩余时间、进度环。
- 主按钮：开始、暂停；辅助按钮：完成、刷新任务。
- 后续待办列表：按时间排序显示下一项任务和预计开始时间。
- 今日节奏卡：能量分数、当前情绪、认知负荷建议、恢复时间。
- 日程救援摘要：无冲突时显示“按原计划推进”；有风险时显示风险数量和“查看日历”入口。
- 协作窗口卡：最佳协作时间、空闲人数；未接入时显示“协作窗口待接入”。
- 时间晶体推荐：可用碎片时间、推荐微任务、一键插入。

#### 关键交互

- 开始/暂停/完成任务后刷新计时和当前任务状态。
- 点击“查看完整日历”跳转智能日历并定位当天。
- 点击关怀提示“查看建议”展开低压建议；关闭按钮只隐藏当前提示。
- 时间晶体的“一键插入”写入日程，成功后显示轻量提示，失败显示明确错误。

#### 状态

- 加载：主卡和列表显示骨架。
- 无任务：显示“今天没有更多日程”和进入日历/微任务的动作。
- 能量不可用：保留任务内容，能量卡显示“暂无能量数据”。
- 加载失败：显示“当前数据可能不完整”和重试按钮，不清空已有数据。

### 5.2 智能日历 `/schedule`

#### 页面目的

查看、编辑和救援日程。日历是本产品的核心工作区，必须支持日/周/月/甘特视图和手动/智能模式。

#### 主界面

- 顶部日期导航：上一天/周/月、今天、当前日期范围。
- 视图切换：日、周、月、甘特；使用分段控件。
- 模式切换：手动安排 / 智能规划。
- 主动作：添加紧急任务并重新规划。
- 辅助动作：新增日程、刷新、导入 ICS、导出 ICS、更多。
- 今日节奏状态条：能量、恢复缓冲、日程数量、冲突数量、数据源。
- 时间地图：左侧时间刻度，中央时间块；显示标题、标签、状态、提醒、目标关联。
- 右侧救援摘要（桌面）：当前冲突、最近一次采用/撤销、移动任务数、撤销入口。

#### 视图要求

- 日视图：精确时间轴，适合拖动和查看任务细节。
- 周视图：按天分列，显示密度和空档。
- 月视图：按日期显示任务数量、风险和完成状态。
- 甘特视图：按任务条带显示持续时间、依赖和截止压力。
- 字段筛选菜单：时间、标签、状态、提醒、目标可独立开关。

#### 模式要求

- 手动模式：允许用户新增、删除和调整普通日程。
- 智能规划：显示“重新规划”动作；算法只在硬约束内调整软目标。
- 不要把智能规划结果直接写入，必须先展示方案并等待确认。

#### 更多菜单

- 目标。
- 插入紧急任务（小屏或智能模式时）。
- 字段显示开关：时间、标签、状态、提醒、目标。
- 导出 iCal（ICS）。
- 导入 iCal（ICS）。
- 情绪与能量。
- MCP 接入。

#### 日程块交互

- 点击进入日程详情/操作上下文，可执行删除。
- 删除前显示确认弹窗，说明任务标题。
- 新增普通日程使用“新增日程”弹窗。
- 重复规则支持不重复、每天、每周、每月，并可设置截止日期。

#### 状态

- 首次加载：时间地图骨架。
- 无日程：显示空时间轴和“新增日程/添加紧急任务”。
- 本地模式：顶部徽标说明本地兜底，保留日程操作。
- 规划冲突：显示问题数量、风险色和进入救援方案的入口。
- 保存失败：原计划保持不变，给出重试，不显示虚假的“已采用”。

### 5.3 微任务页 `/micro`

#### 页面目的

管理短时任务，支持 AI/规则推荐、导入、批量操作和集中安排。

#### 主界面

- 顶部标题“微任务池”、刷新、批量模式、导入清单。
- 推荐横幅：根据当前碎片时间推荐微任务，提供“一键填充”。
- 任务列表：标题、标签、预计分钟、优先级、积分、完成状态。
- 主动作：添加微任务。
- 批量模式工具栏：已选数量、集中安排、批量完成、批量删除、退出批量。
- 空状态：暂无微任务，提供“添加微任务”和“导入清单”。

#### 任务操作

- 新增：标题、标签、预计时长、优先级。
- 编辑：修改同一字段并保存。
- 完成：写入完成事件并更新积分。
- 删除：单项删除前确认。
- 批量安排：要求选中的未完成任务拥有相同标签；生成标题、总时长、日期和开始时间，再写入日程。
- 批量删除：确认数量后删除。
- 批量完成：完成所有选中未完成任务并显示获得积分。

#### 状态

- 导入解析失败：保留原文本，显示格式提示。
- 选中为空：批量动作禁用或提示“请先选择微任务”。
- 标签不一致：集中安排阻止提交并说明原因。
- 写入失败：任务列表保持原状态，显示重试。

### 5.4 团队页 `/team`

#### 页面目的

显示团队成员、共享边界、黄金协作窗口和冲突探测结果，降低会议协调成本。

#### 主界面

- 顶部标题、添加成员、冲突检查、刷新。
- KPI 区：团队人数、当前空闲人数、黄金窗口数量、忙碌重叠数。
- 黄金协作窗口卡：推荐日期时间、持续分钟、空闲人数、推荐原因、发起预约。
- 成员列表：姓名、角色、能量、共享权限、忙碌块数量、当前任务、进度。
- 项目进度追踪：任务标题、负责人、进度条、截止时间、高能量标记。
- 今日合并团队日程：按成员合并时间块。
- 图例：黄金窗口、忙碌重叠、冲突探测。

#### 协作权限

- 不共享。
- 仅空闲/忙碌。
- 共享详情。

根据权限隐藏任务标题和标签；不允许通过视觉猜测隐藏信息。

#### 关键交互

- 点击黄金窗口进入预约确认流程，成功显示预约时间。
- 点击成员进度打开修改进度弹窗。
- 点击忙碌块打开成员、开始、时长和标签详情；权限不足时只显示“忙碌”。
- 小屏将添加成员和冲突检查收进更多菜单。

#### 状态

- 无成员：显示“未找到团队成员”和添加成员。
- 无黄金窗口：说明暂未找到共同空档，提供调整条件/稍后重试。
- 无冲突：冲突区域显示“暂无冲突”。
- 数据加载失败：保留页面骨架和重试入口。

### 5.5 我的页 `/profile`

#### 主界面

- 用户卡：头像、昵称、登录/游客状态；点击昵称可修改，点击头像选择图片。
- AI 认知效率画像卡：当前为待接入状态，点击打开说明弹窗。
- 强调色选择器：提供可识别色板，不使用纯文字模拟色彩。
- 核心操作：设备连接、第三方授权（MCP）、目标。
- 洞察分析：情绪与能量、智能复盘。
- 系统入口：诊断。
- 右上角设置图标打开设置抽屉。

#### 状态

- 游客：显示游客状态和切换账户入口。
- Web 预览：设备入口显示无法访问设备的说明。
- 待接入能力：使用待接入弹窗，不显示空白分析图。

## 6. 二级页面规格

### 6.1 情绪与能量 `/emotion`

- 顶部显示当前情绪、今日打卡次数和刷新。
- 情绪选项：平静、专注、疲惫、烦躁等，以颜色和文字双重表达。
- 今日记录列表：时间、情绪、可选备注。
- 自适应建议：低能量/疲惫时建议低压任务或休息 5 分钟。
- 打卡后显示轻提示；如果生成关怀建议，打开情绪关怀弹窗。
- 空状态：今天还没有记录，直接显示情绪按钮。

### 6.2 目标 `/goals`

- 顶部标题、刷新、新建目标。
- 目标概览：目标数量、完成任务数、临近截止目标。
- 目标卡：标题、截止日期、优先级、完成进度、任务拆解数量。
- 点击目标打开可拖拽高度的目标详情 Bottom Sheet。
- 详情展示任务链、依赖、认知负荷、时长和“安排下一个任务”。
- 所有任务完成时显示完成状态；无合适空档时明确提示。
- 删除目标必须确认，不能静默删除。

### 6.3 智能复盘 `/review`

- 顶部标题、上一周期、当前周期、下一周期。
- 周/月分段切换。
- 操作：模拟一周、生成报告。
- 指标：完成率、计划总时长与实际总时长。
- 分析区：实际时长分布、延误归因、规则建议。
- 调度参数区：默认时长倍率、低能量高负荷惩罚、标签倍率。
- 日程救援记录：策略、采用/撤销状态、移动数量、时间。
- 无报告：提示先模拟或生成；生成失败显示重试。
- 模拟写入失败时不生成报告，不留下部分模拟事件。

### 6.4 MCP 接入 `/integrations`

- 多行文本输入区，支持 ICS、Outlook、Email 等外部文本。
- “解析”按钮识别来源、UID、标题、开始时间、时长、标签。
- 解析预览卡：导入前展示完整字段。
- “导入”仅在解析成功后可用；导入成功显示数量和来源。
- 解析错误显示在输入区下方，保留原内容。

### 6.5 蓝牙页 `/bluetooth`

- 顶部标题和扫描/停止扫描图标。
- 扫描结果：设备名、RSSI、远端 ID、连接/断开按钮。
- 已连接设备可点击进入设备详情。
- 无设备：显示“未找到设备”和重新扫描。
- Web/不支持平台：显示蓝牙不可用说明，不显示假扫描结果。

### 6.6 设备详情 `/bluetooth/:deviceId`

- 顶部显示设备名、连接状态、刷新服务。
- 服务使用可展开列表显示 UUID。
- 特征值显示 UUID、属性和操作图标：读取、写入、通知开关。
- 读取后打开特征值结果弹窗。
- 写入前打开十六进制输入弹窗；格式非法时显示错误。
- 通知切换显示启用/停用状态。

### 6.7 诊断 `/diagnostics`

- 顶部刷新和导出日志。
- 数据源：数据服务、本地服务、远端服务类型。
- 本地存储：后端、是否存在、字节大小。
- 调度性能：重规划触发次数、上次耗时、时间、原因。
- ICS：上次导入/导出结果、数量、路径、错误。
- 日志区：级别、类别、消息、时间和结构化数据。
- 操作：清空日志、导出日志；导出失败显示明确错误。

## 7. 弹窗、抽屉与 Bottom Sheet 清单

以下每一项都必须作为独立 Stitch 画板或可复用状态生成。桌面/平板默认使用 Dialog 或右侧 Drawer，手机默认使用 Bottom Sheet；字段和主动作保持一致。

| ID | 名称 | 触发入口 | 内容与操作 | 移动端形态 |
| --- | --- | --- | --- | --- |
| O-01 | 启动登录浮层 | 首次打开且无当前用户 | 登录、注册、游客进入、关闭；登录成功返回原工作台 | 全屏/底部登录面板 |
| O-02 | 语言选择 | 登录浮层或设置 | 简体中文、English | Dialog |
| O-03 | 新增日程 | 日历新增按钮 | 标题、标签、时长、颜色、开始时间、提醒、重复规则、重复截止日期；取消/新增 | Bottom Sheet |
| O-04 | 插入紧急任务 | 日历主动作 | 标题、预计分钟、优先级、认知负荷、截止日期、截止时间；截止时间必须晚于当前时刻 | Bottom Sheet |
| O-05 | 救援方案比较 | 紧急任务提交后 | 三种方案卡、差异时间线、移动数量、恢复缓冲、逾期风险、问题列表；采用某方案/暂不调整 | 全屏 Dialog / Bottom Sheet |
| O-06 | 救援保存失败 | 采用方案写入失败 | 说明原计划未改变、重试、关闭 | Dialog |
| O-07 | ICS 导入 | 日历更多菜单 | ICS 多行文本、格式帮助、预览/导入、取消 | Bottom Sheet |
| O-08 | 删除日程确认 | 日程块删除 | 显示任务标题；取消/删除 | Dialog |
| O-09 | 导入微任务 | 微任务导入 | 多行清单输入、预览解析结果、任务数量、总分钟、积分、取消/预览/导入 | Bottom Sheet |
| O-10 | 新增微任务 | 微任务主动作 | 标题、标签、预计时长、优先级；取消/添加 | Bottom Sheet |
| O-11 | 编辑微任务 | 微任务卡编辑 | 同新增字段；取消/保存 | Bottom Sheet |
| O-12 | 删除微任务确认 | 微任务卡删除 | 显示任务标题；取消/删除 | Dialog |
| O-13 | 批量删除确认 | 批量工具栏 | 显示选中数量；取消/删除 | Dialog |
| O-14 | 同类任务集中安排 | 批量安排 | 标题、总分钟、日期、开始时间、标签只读；取消/加入日程 | Bottom Sheet |
| O-15 | 添加团队成员 | 团队添加成员 | 姓名、角色、当前任务；取消/添加 | Bottom Sheet |
| O-16 | 冲突检查条件 | 团队冲突检查 | 开始时间、会议时长；取消/检查 | Bottom Sheet |
| O-17 | 冲突结果 | 冲突检查后 | 无冲突或忙碌成员列表；确定 | Dialog |
| O-18 | 修改成员进度 | 点击成员任务 | 百分比数字、滑块、取消/保存 | Bottom Sheet |
| O-19 | 团队忙碌块详情 | 点击合并日程块 | 成员、开始、时长、标签；权限不足仅显示忙碌；关闭 | Dialog |
| O-20 | 新建目标 | 目标页主动作 | 标题、截止日期、优先级、自动拆解提示；取消/新建 | Bottom Sheet |
| O-21 | 删除目标确认 | 目标卡删除 | 显示目标标题；取消/删除 | Dialog |
| O-22 | 目标详情 | 点击目标卡 | 目标摘要、任务链、依赖、进度、安排下一个任务、删除 | Draggable Bottom Sheet |
| O-23 | 情绪关怀建议 | 情绪打卡后 | 当前情绪、建议低压任务/休息动作、确认 | Dialog |
| O-24 | 待接入能力说明 | 认知画像、通知等 | 标题、当前未接入说明、关闭 | Dialog |
| O-25 | 不支持设备说明 | Web 或不支持蓝牙平台 | 不支持原因、支持平台提示、确认 | Dialog |
| O-26 | 修改昵称 | 点击用户卡昵称 | 新昵称输入；取消/保存 | Bottom Sheet |
| O-27 | 设置抽屉 | 我的页设置图标 | 切换账户、语言、通知状态、外观模式、强调色、关闭 | 桌面右侧 Drawer；手机 Bottom Sheet |
| O-28 | 特征值读取结果 | 设备详情读取 | UUID、返回值、确定 | Dialog |
| O-29 | 特征值写入 | 设备详情写入 | 十六进制输入、格式提示、取消/写入 | Bottom Sheet |

### 7.1 救援方案卡的统一字段

每种方案必须展示：

- 策略名：优先保住截止时间、优先保留恢复时间、尽量少动原计划。
- 一句话适用场景。
- 新增、移动、保留、可能逾期的任务数量。
- 恢复缓冲分钟数；恢复策略默认 15 分钟。
- 最早完成时间和逾期数量。
- 差异时间线：移动前后用颜色和箭头表达。
- 解释标签：截止接近、优先级、精力匹配、保留原计划、固定冲突。
- 明确的“采用此方案”按钮，不允许点击卡片就直接提交。

### 7.2 设置抽屉内容

- 顶部：设置标题、关闭按钮。
- 账户：切换账户。
- 语言：简体中文 / English。
- 通知：当前为待接入状态，点击显示说明弹窗。
- 外观：系统、浅色、深色分段控件。
- 强调色：色板选择，选中态可见。
- 抽屉宽度桌面 360-460px，从右侧滑入；手机高度至少 70vh，支持安全区和滚动。

## 8. 核心业务规则

### 8.1 调度规则

- 固定日程和不可占用区间优先级最高。
- 硬截止时间不能被静默突破；无法满足时返回 `miss_due`、`overdue` 或 `no_slot` 问题。
- 三种策略只改变软目标权重，不绕过硬约束。
- 保护截止时间：优先满足截止，允许移动更多普通任务。
- 保护恢复时间：偏好精力匹配并预留 15 分钟缓冲。
- 最少改动：尽量锁定原有日程，只使用现有空档。

### 8.2 数据可靠性

- 远端成功后显示已同步；缓存同步失败不诱导用户重复提交。
- 远端不可用、网络超时或 5xx 才进入本地模式。
- 4xx、鉴权失败、冲突和响应格式错误必须明确显示。
- 救援采用和撤销在一个事务中完成；成功才显示“已采用”。
- 撤销恢复原排期并写入撤销事件；复盘页面可以按周期查看采用/撤销历史。
- 批量模拟一周写入失败时，不生成部分事件和报告。

### 8.3 ICS 与外部文本

- ICS 导入按本地时区处理，当前版本忽略 RRULE 重复规则。
- 导入前显示预览，避免直接写入不可见数据。
- 外部文本解析失败时保留用户输入并显示可修复提示。

## 9. 全局状态与反馈

每个页面都需要设计以下状态变体：

1. 正常有数据。
2. 首次加载骨架。
3. 刷新中但保留旧数据。
4. 空数据与下一步动作。
5. 本地模式。
6. 需要登录。
7. 校验错误。
8. 冲突/风险。
9. 保存失败且数据未改变。
10. 成功反馈与撤销入口。

反馈优先级：

- 页面级错误：状态横幅 + 重试。
- 字段级错误：紧贴字段显示，不只依赖颜色。
- 成功操作：浮动 Snackbar 或页面内成功徽标。
- 可撤销操作：成功反馈中直接提供“撤销”。
- 长时间任务：显示进度，不允许按钮无反馈地等待。

## 10. 无障碍与响应式验收

- 所有文字、按钮、标签在 375px 手机宽度下不溢出、不遮挡。
- 主要按钮、列表项和关闭按钮满足最小触控区域。
- 键盘焦点顺序与视觉顺序一致，焦点环清晰可见。
- 图标按钮提供语义名称和 tooltip，不能只依赖图标形状。
- 错误、完成、风险同时使用文字/图标/颜色表达。
- 减少动效模式下直接呈现最终状态；不使用持续背景动画。
- Dialog、Drawer、Bottom Sheet 打开后焦点进入容器，关闭后返回触发控件。
- 手机 Bottom Sheet 需要拖拽把手、安全区底部内边距和滚动区域。
- 桌面 1440px、平板 1024px、手机 390px 均不得出现横向滚动。

## 11. Stitch 生成要求

请将本 PRD 作为一个完整产品系统生成，不要只生成单一落地页。输出顺序：

1. 先生成全局壳层和设计令牌。
2. 再生成五个一级页面的桌面、平板、手机版本。
3. 再生成二级页面。
4. 最后生成第 7 节中的全部弹窗、抽屉和 Bottom Sheet。
5. 对每个页面补齐加载、空、错误、本地模式和冲突状态。

### 11.1 Stitch 页面描述模板

每个画板都要标注：

- 页面 ID 和页面名称。
- 设备尺寸和主题。
- 进入条件与触发入口。
- 页面层级和主要区块。
- 主动作、次动作和取消路径。
- 数据状态和错误文案。
- 与其他页面/弹窗的跳转关系。

### 11.2 推荐生成顺序与画板命名

```text
00-shell-desktop-light
01-shell-tablet-light
02-shell-mobile-light
03-focus-default
04-calendar-default
05-calendar-rescue-comparison
06-micro-default
07-team-default
08-profile-default
09-emotion-default
10-goals-default
11-review-default
12-integrations-default
13-bluetooth-default
14-device-detail-default
15-diagnostics-default
overlay-01-auth
overlay-02-add-schedule
overlay-03-urgent-task
overlay-04-rescue-mobile-sheet
overlay-05-settings-drawer
overlay-06-micro-import
overlay-07-goal-detail-sheet
overlay-08-team-conflict
overlay-09-device-write
```

### 11.3 Stitch 文案约束

- 默认中文文案使用本文档和仓库中的中文字符串，不自行发明营销口号。
- 主要动作使用动词：开始、暂停、完成、添加、采用、撤销、导入、导出、重试、关闭。
- 风险文案必须说明影响和下一步动作，例如“有 2 项任务无法在截止前完成，查看方案”。
- “待接入”只用于真实未实现能力；不使用假数据填充图表。

## 12. 验收清单

- [ ] 五个一级页面可从全局导航互达。
- [ ] 情绪、目标、复盘、MCP、蓝牙、设备详情、诊断页面均有独立界面。
- [ ] 所有 29 个弹窗/抽屉/Bottom Sheet 均有画板或复用组件规格。
- [ ] 紧急任务救援包含截止校验、三方案比较、确认、成功反馈、撤销和复盘历史。
- [ ] 普通日程支持新增、提醒、重复、删除确认。
- [ ] 微任务支持新增、编辑、完成、删除、导入预览和批量操作。
- [ ] 团队支持成员、权限、黄金窗口、预约、冲突检查和进度更新。
- [ ] 设置支持语言、主题、强调色、账户切换和待接入通知说明。
- [ ] 每个页面包含加载、空数据、错误、本地模式和冲突/风险状态。
- [ ] 桌面、平板、手机以及浅色/深色主题均无布局溢出。
- [ ] 不把本地数据标记为实时远端数据，不把未接入能力表现为已完成。
