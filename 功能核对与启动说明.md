# 功能核对与启动说明

## 一键启动 Chrome

在 VS Code 终端进入项目根目录后执行：

```bat
.\run_chrome.bat
```

也可以直接双击项目根目录下的 `run_chrome.bat`。

脚本会自动：

1. 检查 `python` 和 `flutter` 是否可用。
2. 检查 `http://127.0.0.1:8000/health`。
3. 如果后端没启动，自动打开一个后端窗口并运行 FastAPI。
4. 执行 `flutter pub get`。
5. 用 Chrome 启动前端，并传入 `API_BASE_URL=http://127.0.0.1:8000`。

只检查后端不打开 Chrome：

```bat
.\run_chrome.bat --check
```

## 比赛主线补充验收

- 紧急任务要求填写精确截止日期和时间；缺失、格式无效或已经过期的截止点不能提交。
- 系统一次给出三种可解释的日程救援策略，用户比较后再确认；确认会持久化更新日程，撤销会恢复排期。
- 接受与撤销都会写入任务事件，周/月复盘页可见日程救援历史，而不是只保留最终日程结果。
- 模拟一周任务时，页面只执行一次 `upsertTaskEvents` 批量写入。批量失败时不产生模拟事件、不生成报告，加载控件恢复，并只显示一次本地化错误提示。

## 数据服务边界

- 前端保持 Flutter/Dart，后端保持 FastAPI 与 SQLite；本次版本没有迁移到 Node.js。
- 远端写入成功后，本地缓存同步为尽力而为，避免缓存失败诱导用户重复提交。
- 远端不可用、网络超时或服务端 5xx 时才会落到本地；4xx、冲突、校验失败和响应格式错误会原样暴露给调用方。
- `POST /events/batch` 支持幂等批量事件写入，拒绝空 ID、批内重复 ID 及跨用户事件 ID 冲突。

## 当前交付验证

最终交付以本轮最新命令输出为准：

```text
flutter analyze
flutter test -r compact
F:\Tools\Python310\python.exe -m pytest backend\tests -q
```

本轮最新验证结果：`flutter analyze` 无诊断，Flutter 测试 109 项全部通过；后端测试 10 项通过，保留 3 条 Starlette/httpx 依赖弃用警告，不影响当前运行。

## 当前功能核对

### 比赛主线：日程崩溃救援

在智能日历中新增紧急任务时，系统不会直接覆盖原有日程，而是生成三种方案供用户比较：

- 优先保住截止时间：优先安排紧急事项，允许移动更多普通任务。
- 优先保留恢复时间：降低高负荷安排倾向，并预留 15 分钟恢复缓冲。
- 尽量少动原计划：锁定已有日程，只尝试使用现有空档。

方案卡片会展示原因、代价、移动任务数量和恢复缓冲。用户确认后才写入日程，且支持撤销；
接受、撤销等操作会写入 `TaskEvent`，用于后续复盘。该流程是本项目比赛演示和验收的核心闭环。

保留的前端主功能：

- 专注页：当前任务、下一任务、情绪/能量提示、专注计时、微任务推荐。
- 智能日历：日/月/甘特视图、日程增删改、重排、重复规则、提醒、目标关联。
- 微任务：新增/编辑/完成/批量导入、积分、优先级解析、碎片时间推荐。
- 团队协作：成员日程、黄金协作窗口、冲突检测、会议预订、共享权限。
- 个人中心：登录/注册/游客、头像昵称、设备入口、目标、情绪、复盘、集成、诊断。
- 原登录界面和毛玻璃效果：已恢复启动展示，并增加关闭按钮，避免阻塞主流程。

已接入独立后端的功能：

- 账号注册、登录、当前用户查询。
- 日程列表、新增、更新、删除。
- 微任务列表、新增、更新、删除。
- 日程智能重排算法接口。
- 任务执行事件接口：`POST /events`、`GET /events`。
- 微任务碎片时间推荐接口。

仍采用本地兜底的功能：

- 情绪记录、能量状态、目标管理、团队协作详细数据、复盘报告、提醒设置、设备偏好。

这些本地兜底功能没有被删除；后端不可用时也能继续使用基础功能。

## 已执行验证

```text
flutter analyze
No issues found

flutter test -r compact
All tests passed

python -m pytest backend/tests -q
5 passed, 1 warning

GET http://127.0.0.1:8000/health
{"ok":true,"version":"0.1.0","database":"sqlite"}
```

后端测试中的 warning 是 Starlette/httpx 测试库弃用提醒，不影响项目运行。

## 当前版本边界

当前版本以本地优先的启发式调度为主，重点验证“突发任务 - 方案比较 - 用户确认 - 可撤销 - 事件记录”
这条真实业务链路。多模态生理信号、GPU 模型、第三方办公软件深度监听和大规模并发指标属于后续研究方向，
在没有专项数据集、压测报告或设备授权前，不作为当前版本的实测结论。
